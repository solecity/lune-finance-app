// libraries
import styled from "styled-components";

// external components
import Grid from "@mui/material/Grid";

const StyledTabs = styled(Grid)`
  margin-bottom: 40px;
`;

export { StyledTabs };
