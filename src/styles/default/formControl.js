// libraries
import styled from "styled-components";

// external components
import FormControl from "@mui/material/FormControl";

export const StyledFormControl = styled(FormControl)`
  margin-bottom: ${({ theme }) => theme.spacing.s10} !important;
`;
